/**
 * Whether the current user has Tomo Plus.
 *
 * There are no accounts yet, so this is the single stub every Plus gate reads.
 * When sign-in lands, replace it with the real entitlement and the gates keep
 * working unchanged.
 */
export const IS_PLUS = false
