/**
 * Whether the current user has Tomo Pro.
 *
 * There are no accounts yet, so this is the single stub every Pro gate reads.
 * When sign-in lands, replace it with the real entitlement and the gates keep
 * working unchanged.
 */
export const IS_PRO = false
